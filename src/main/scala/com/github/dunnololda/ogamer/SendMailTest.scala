package com.github.dunnololda.ogamer

import scala.concurrent.duration._

object SendMailTest extends App {
  /*sendEmailSync(EmailMessage(
    subject = "ogame report",
    recipient = "ogameinformer7@gmail.com",
    from = "ogameinformer7@gmail.com",
    text = "test message",
    html = "test message",
    smtpConfig = SmtpConfig(
      tls = true, ssl = false, host = "smtp.gmail.com", port=  587, user = "ogameinformer7", password = "lienajava"
    ),
    retryOn = 1.minute,
    deliveryAttempts = 5
  ))*/
  //sendMailSimple("ogameinformer7@gmail.com", "ogame report", "test")
}
